{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "title": "Revenue Seating Chart",
  "description": "Revenue seating map of employee's and contractors",
  "type": "object",
  "properties": {
    "pName": {
      "description": "Name of a person",
      "type": "string",
      "pattern": "^[a-zA-Z]+$",
      "minLength": 3,
      "maxLength": 30
    },
    "pSurname": {
      "description": "Surname of a person",
      "type": "string",
      "pattern": "^[a-zA-Z]+$",
      "minLength": 3,
      "maxLength": 30
    },
    "pTitle": {
      "description": "Job title of a person",
      "type": "string",
      "pattern": "^[a-zA-Z]+$",
      "minLength": 3,
      "maxLength": 255
    },
    "pDepartment": {
      "description": "Department in which person is working",
      "type": "string",
      "pattern": "^[a-zA-Z]+$",
      "minLength": 3,
      "maxLength": 50
    },
    "pSeatNo": {
      "description": "Seat number",
      "type": "integer",
      "minimum": 0,
      "maximum": 1999999
    },
    "pFloor": {
      "description": "Floor",
      "type": "string",
      "enum": [
        "GROUNDFLOOR",
        "FIRSTFLOOR",
        "SECONDFLOOR",
        "THIRDFLOOR"
      ]
    },
    "pCompany": {
      "description": "Companies name",
      "type": "string",
      "pattern": "^[a-zA-Z]+$",
      "minLength": 3,
      "maxLength": 255
    }
  },
  "required": [
    "pName",
    "pSurname",
    "pCompany",
    "pDepartment",
    "pFloor",
    "pSeatNo",
    "pTitle"
  ]
}